---
/**
 * Waitlist Success Page
 * Shown after email is submitted (before confirmation).
 */
import '../../styles/global.css';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Check Your Email - Accountability Bet</title>
    <meta name="description" content="Almost there! Check your email to confirm your spot on the Accountability Bet waitlist." />
    <meta name="robots" content="noindex" />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  </head>

  <body class="min-h-screen bg-gradient-to-br from-purple-600 via-purple-700 to-indigo-800 flex items-center justify-center px-4">
    <div class="max-w-md w-full text-center">
      <!-- Success Icon -->
      <div class="mb-8">
        <div class="w-20 h-20 mx-auto bg-white/20 backdrop-blur-sm rounded-full flex items-center justify-center">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
        </div>
      </div>

      <!-- Content -->
      <h1 class="text-3xl md:text-4xl font-extrabold text-white mb-4">Check Your Email</h1>
      <p class="text-lg text-white/80 mb-8">
        We sent you a confirmation link. Click it to secure your spot on the Accountability Bet waitlist.
      </p>

      <!-- Tips Card -->
      <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-2xl p-6 mb-8">
        <h2 class="text-white font-semibold mb-4">Don't see the email?</h2>
        <ul class="text-left text-white/80 text-sm space-y-2">
          <li class="flex items-start gap-2">
            <svg class="w-5 h-5 text-white/60 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
            <span>Check your spam or junk folder</span>
          </li>
          <li class="flex items-start gap-2">
            <svg class="w-5 h-5 text-white/60 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
            <span>Look for an email from noreply@accountabilitybet.ch</span>
          </li>
          <li class="flex items-start gap-2">
            <svg class="w-5 h-5 text-white/60 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
            <span>The link expires in 24 hours</span>
          </li>
        </ul>
      </div>

      <!-- Back Link -->
      <a href="/" class="text-white/60 hover:text-white transition-colors text-sm">
        Back to homepage
      </a>
    </div>

    <style>
      body {
        font-family: 'Plus Jakarta Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      }
    </style>
  </body>
</html>
